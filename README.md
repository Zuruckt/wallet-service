Based on https://github.com/madsonar/setup-create-project-hyperf-framework-php-swoole-digital-codigos

Needs the followings enviroment variables:
```bash
export USER=$(id -un)
export UID=$(id -u)
export GID=$(id -g)
```
